<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primaryColor">

    <!-- Background Image -->
    <ImageView
        android:id="@+id/backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/img"
        android:scaleType="centerCrop"
        android:contentDescription="Background Image"/>

    <!-- Your other views on top of the background -->


    <!-- App Logo -->
    <ImageView
        android:id="@+id/logoImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"
        android:src="@drawable/logo"
        android:contentDescription="@string/app_name" />

    <!-- Optional: Add a tagline or app name -->
    <TextView
        android:id="@+id/taglineText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_below="@id/logoImage"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="@string/tagline"
        android:textSize="18sp"
        android:textColor="#051887"
        android:fontFamily="sans-serif-medium" />


    <TextView
        android:id="@+id/developer"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/taglineText"
        android:layout_alignStart="@+id/taglineText"
        android:layout_alignEnd="@+id/taglineText"
        android:layout_centerInParent="true"
        android:layout_marginStart="15dp"
        android:layout_marginTop="190dp"
        android:layout_marginEnd="-14dp"
        android:fontFamily="sans-serif-medium"
        android:hint="Developed By Mihir and Kunj"
        android:textColor="@android:color/white"
        android:textSize="16sp" />

</RelativeLayout>
